name: Job Dependency Example

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build steps
        run: echo "Building the project..."

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test steps
        run: echo "Running tests..."